{
  "name": "Luces Belen - ESP32",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "forwardPorts": [80, 443, 3000],
  "customizations": {
    "vscode": {
      "extensions": [
        "platformio.platformio-ide",
        "ms-vscode.cpptools"
      ]
    }
  },
  "postCreateCommand": "sudo usermod -aG dialout codespace && sudo apt-get update && sudo apt-get install -y python3-pip && pip3 install esptool pyserial",
  "remoteUser": "codespace"
}